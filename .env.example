# Server
PORT=4000

# Database (PostgreSQL)
DB_HOST=192.168.18.10
DB_PORT=5433
DB_USER=gastometro
DB_PASSWORD=gastometro
DB_NAME=gastometro

# JWT
JWT_SECRET=changeme
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN_DAYS=7
JWT_IDLE_TIMEOUT_MINUTES=60

# Password hashing
BCRYPT_SALT_ROUNDS=10

# Rate limiting
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=60

# Cache
CACHE_TTL_SECONDS=60
CACHE_MAX_ITEMS=500
